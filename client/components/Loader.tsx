import React from 'react';
import 'twin.macro';

const Loader: React.FC = () => {
  return (
    <div tw="flex items-center justify-center h-screen text-center align-middle">
      Loading...
    </div>
  );
};

export default Loader;
